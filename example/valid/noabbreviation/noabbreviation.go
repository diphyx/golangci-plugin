// Package noabbreviation shows full identifier names instead of abbreviations.
package noabbreviation

// Handle uses full names instead of abbreviations like req/resp.
func Handle(request string) string {
	response := request

	return response
}
